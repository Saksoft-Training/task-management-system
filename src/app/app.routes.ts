import { Routes } from '@angular/router';
import { DashboardNotificationsComponent } from './features/dashboard and reports/components/notifications/dashboard-notifications-component/dashboard-notifications-component';

export const routes: Routes = [

];
